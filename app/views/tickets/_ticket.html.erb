<%= form_for ticket, remote: true do |f| %>
  <%= f.label :complete, ticket.description %> |
  <%= link_to "Delete", ticket,
                        data: {confirm: "Are you sure?", method: :delete},
                        remote: true %>
<% end %>
